app.controller('users.editController', function($scope) {
	$scope.redirect();
});

app.controller('users.newController', function($scope) {
});

app.controller('users.profileController', function($scope, $modal, $log) {
	$scope.redirect();
});
